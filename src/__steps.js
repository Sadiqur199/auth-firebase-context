/**
 * use context api
 * 1.create context (export)
 * 2.create provider 
 * 3.set context value 
 * 4.to access the value : use context hook 
 */

/**
 * 1.main.jsx set AuthProvider
 * 2. inside provider access  children props  and the use 
*/


/**
 * 1.break down the create user function
 * 2.auth provider will have auth and pass email form register
*/